# Basic React-Webpack-Typescript setup

This project contains basic setup for React-Webpack-Typescript with live reload.

To run the application run the below commands in order:
  - `npm install`
  - `npm run typings`
  - `npm start`